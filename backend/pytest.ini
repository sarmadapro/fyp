[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    -v
    --tb=short
    --strict-markers
    -p no:warnings

markers =
    slow: mark tests that are slow (e.g. use real LLM)
    integration: mark tests requiring external services (STT, TTS, Ollama)
